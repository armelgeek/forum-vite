export const apiPrefix = {
    baseUrl: "http://localhost:8100"
}
export const storageDataPrefix = {
    accessToken: "@user_info_token",
    refreshToken: "@user_info_refresh_token",
    setting: "@setting_v1",
    theme: "@theme"
} as const;